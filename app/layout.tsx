import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geist = Geist({
  subsets: ["latin"],
  variable: "--font-geist",
  display: "swap",
});

const geistMono = Geist_Mono({
  subsets: ["latin"],
  variable: "--font-geist-mono",
  display: "swap",
});

export const metadata: Metadata = {
  title: "rhythm.ui — Beautiful Animated Components",
  description:
    "Beautiful, animated UI components for React, HTML & CSS. Free forever. Copy, paste, ship.",
  keywords: ["UI library", "React components", "animated components", "free UI"],
  openGraph: {
    title: "rhythm.ui",
    description: "Copy. Paste. Ship. Beautiful animated components, free forever.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${geist.variable} ${geistMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
